# Necesario para convertir el audio antes de transcribir
sudo apt install ffmpeg 

# Necesario para las ventanas de selección de archivos bonitas
sudo apt install zenity   

# A veces necesario si tkinter da error al iniciar
sudo apt install python3-tk 


sudo pacman -S ffmpeg zenity tk
